<%
	meta("../../meta.json")
	meta()
	const path = require('path');
	url = url + "/posts/" + path.basename(path.dirname(outputPath)) + "/";
%>
<%= render("../../_partials/post-header.html", { title, image: null, caption: "", url }) %>

Welcome to my new site. This is the first post.

## Why this exists

I wanted a small, fast, own-it-forever place to write things down. So here we are.

## What it's built with

This site is generated by [blargh](https://github.com/badlogic/blargh), a tiny static site generator. The whole thing is a folder of Markdown, a sprinkle of HTML, a bit of CSS, and a few custom elements. No framework, no build pipeline beyond blargh itself.

## Code looks like this

```ts
function hello(name: string) {
    return `Hello, ${name}!`;
}

console.log(hello("world"));
```

## What's next

More posts. Probably about tools I'm building, things I've broken, and notes to my future self.

<%= render("../../_partials/post-footer.html", { title, url }) %>
