## 2026-04-17.00

**Completed:**
- Scaffolded full blargh-based static site at `~/Projects/nikkopatten.com/` (derived from [badlogic/mariozechner.at](https://github.com/badlogic/mariozechner.at), MIT). Build verified (`./build.sh` → `html/` with working `rss.xml`). Initial git commit made on `main`.

**Decisions:**
- Chose `nikkopatten.com` as canonical domain over `.tech` / `.net` / `nikko-patten.com` (.com is still what people type by default; other domains should 301 to this one).
- Kept structure identical to upstream (Mario's repo) rather than restructuring, so upstream improvements and the reference `AGENTS.md` stay easy to port.
- Removed Mario's HN-referrer redirect prank from `components.js`. There is no `nothanks.html` page — do not add one unless that behavior is wanted.

**General Notes for future self:**
- `blargh` is installed GLOBALLY via `npm install -g @mariozechner/blargh`. There is intentionally NO `package.json` / `node_modules` in this repo — blargh is a CLI tool, not a dependency. Don't "fix" this by adding one. Reinstall globally on a new machine.
- Files/dirs prefixed with `_` (e.g. `_css/`, `_partials/`, `_icons/`) are NOT emitted as pages — they are only pulled in via `render()` / `meta()`. This is a blargh convention, not a project one.
- `post-header.html` was modified vs upstream: it now conditionally renders the `<figure>` only when `image` is truthy, so posts without a header image don't emit a broken `<img src="">`. Remember this if you ever re-sync from upstream.
- `html/` is fully regenerated by every build and is git-ignored. Never hand-edit it. The deployable artifact IS that folder — any static host works (no deploy pipeline wired up yet).
- Reference source (full, with real posts) is at `/tmp/mariozechner.at` during this session but will NOT persist across reboots. Re-clone from `https://github.com/badlogic/mariozechner.at` if you need to diff or pull in a pattern (e.g. post with header image, video embedding, math, TOC usage).
- Open placeholders intentionally left for the human: (1) bio `TODO` block in `src/index.html`, (2) social links in `src/index.html` (only GitHub is wired; Bluesky/Mastodon are commented stubs), (3) `hello@nikkopatten.com` contact email — set up or change before launch, (4) color palette in `src/_css/colors.css` still matches Mario's orange/blue.
- Hosting is NOT set up. No `publish.sh`, no CI, no DNS. Next session work: pick a host (Cloudflare Pages / Netlify / VPS), wire deployment, configure DNS on the domain registrar, and set up 301 redirects from the other three domains.
- The sample post at `src/posts/2026-04-17-hello-world/` is a working template — keep it as a reference for the exact EJS boilerplate until replaced by a real post. See `AGENTS.md` for the canonical "add a new post" recipe.
